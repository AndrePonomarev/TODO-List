import { createStore } from "vuex";

export default createStore({
    state: {
        count: 0
        //statusses
        //tasks
    },
    // getters: {
    //     squaredCount(state)
    // }
    // mutations: {
    //     increment(state) {
    //         state.count++;
    //     },
    //     deincrement(state) {
    //         state.count++;
    //     }

    //     //
    // },

    actions: {
        increment: ({ commit }) => {
            
        },
        
    }
});